# Wheelp — Security Audit #2 del 2026-08-23

**Sesión:** `cse_0142GbZqDm5N1sbSX3e2Zi4i`  
**Disparada:** manualmente a las 13:42 UTC  
**Estado previo:** commit `02d1121` (migración avatares a Storage)

---

## Resumen

| # | Audit reportó | Estado real al verificar | Acción |
|---|---|---|---|
| 1 | CRITICAL — `help_requests.leer_autenticados` (qual=true) | No existía en live | Falso positivo (ya limpiada en sesión anterior) |
| 2 | CRITICAL — `profiles` INSERT sin `with_check` | `insertar_propio_perfil` YA tenía `with_check` correcto | Sin acción necesaria |
| 3 | CRITICAL — `help_requests.actualizar_propios` | No existía en live | Falso positivo (ya limpiada en sesión anterior) |
| 4 | HIGH — duplicados INSERT/DELETE en `help_requests` | `crear_propias` y `borrar_implicados` existían | **Eliminadas** |
| 5 | HIGH — `update_own_or_helper` WITH CHECK permite cambiar `helper_id` | Confirmado | Anotado — requiere BEFORE UPDATE trigger (pendiente) |
| 6 | HIGH — sin suite de tests / CI | Confirmado | **Cerrado en commit `6859fee`** (Hallazgo 7) |
| 7 | HIGH — sin `supabase/migrations/` | Confirmado | Pendiente (Hallazgo 8) |
| 8 | MEDIUM — `send-push` APNs sandbox | Ver audit anterior | Sin cambio |
| 9 | MEDIUM — `uploadAvatar()` | Cerrado en commit `02d1121` | Aplicado |

---

## Cambios aplicados en esta sesión

### Migración `drop_duplicate_rls_policies`

```sql
DROP POLICY IF EXISTS crear_propias ON public.help_requests;
DROP POLICY IF EXISTS borrar_implicados ON public.help_requests;
```

**Motivo:** PERMISSIVE policies con OR-logic — los duplicados anulaban la granularidad de las políticas más nuevas (`insert_own`, `delete_own_or_helper`).

### Rutina de audit actualizada

Añadido paso obligatorio: al terminar, enviar el informe completo a `aelguer@icloud.com` vía AgentMail MCP. Activo desde el próximo lunes 2026-08-25 a las 07:00 UTC.

---

## Estado RLS tras esta sesión

| Tabla | Políticas activas |
|---|---|
| `help_requests` | `insert_own`, `select_own_or_helper`, `update_own_or_helper`, `aceptar_pendientes`, `delete_own_or_helper` |
| `profiles` | `insertar_propio_perfil`, `leer_propio_perfil` |

---

## Pendientes

| # | Descripción | Prioridad |
|---|---|---|
| A | BEFORE UPDATE trigger en `help_requests` para impedir que el solicitante cambie `helper_id` | HIGH |
| 8 | Directorio `supabase/migrations/` | MEDIUM |
